module.exports = {
  sum(a, b) {
    // English: Define a function named 'sum' that takes two parameters, 'a' and 'b'.
    // It calculates the sum of 'a' and 'b' and logs the result to the console.
    // Português: Define uma função chamada 'sum' que recebe dois parâmetros, 'a' e 'b'.
    // Ela calcula a soma de 'a' e 'b' e exibe o resultado no console.
    console.log(a + b);
  },
}
