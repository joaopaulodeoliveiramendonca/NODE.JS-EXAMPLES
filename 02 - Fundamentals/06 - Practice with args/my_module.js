//Exporta um objeto com métodos a partir deste módulo.
module.exports = {
  //Defina uma função 'sum' que recebe dois parâmetros, 'a' e 'b'. 
  sum(a, b) {
    //Esta função exibe o resultado da soma de 'a' e 'b' no console.
    console.log(a + b);
  },
};
