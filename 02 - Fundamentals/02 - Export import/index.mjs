import sum from "./my_module.mjs"; 
// English: Import the default export from the module './my_module.mjs' and assign it to the variable 'sum'.
// Português: Importe a exportação padrão do módulo './my_module.mjs' e atribua-a à variável 'sum'.

sum(2, 3); 
// English: Call the 'sum' function with arguments 2 and 3. This should execute the function from 'my_module.mjs' and perform the summation.
// Português: Chame a função 'sum' com os argumentos 2 e 3. Isso deve executar a função do 'my_module.mjs' e realizar a soma.
