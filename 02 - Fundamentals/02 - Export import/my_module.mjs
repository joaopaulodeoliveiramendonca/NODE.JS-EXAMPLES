function sum(a, b) { 
  // English: Define a function named 'sum' that takes two parameters, 'a' and 'b'.
  // The function logs the result of adding 'a' and 'b' to the console.
  // Português: Defina uma função chamada 'sum' que recebe dois parâmetros, 'a' e 'b'.
  // A função exibe o resultado da soma de 'a' e 'b' no console.
  console.log(a + b);
}

export default sum; 
// English: Export the 'sum' function as the default export from this module.
// This allows other modules to import 'sum' as the default export.
// Português: Exporte a função 'sum' como a exportação padrão deste módulo.
// Isso permite que outros módulos importem 'sum' como a exportação padrão.
