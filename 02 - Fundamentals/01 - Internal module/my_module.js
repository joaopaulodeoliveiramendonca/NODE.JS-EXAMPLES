module.exports = {
  // English: Export an object with methods from this module.
  // Português: Exporte um objeto com métodos a partir deste módulo.

  sum(a, b) {
    // English: Define a function 'sum' that takes two parameters, 'a' and 'b'.
    // This function logs the result of adding 'a' and 'b' to the console.
    // Português: Defina uma função 'sum' que recebe dois parâmetros, 'a' e 'b'.
    // Esta função exibe o resultado da soma de 'a' e 'b' no console.
    console.log(a + b);
  },
};
