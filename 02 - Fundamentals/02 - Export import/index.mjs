// Importa a função `sum` do arquivo "my_module.mjs"
// O ".mjs" indica que é um módulo ES6, onde podemos usar "import/export"
import sum from "./my_module.mjs";

// Chama a função `sum`, passando os números 2 e 3 como argumentos
// O resultado da soma será calculado dentro dessa função
sum(2, 3);
